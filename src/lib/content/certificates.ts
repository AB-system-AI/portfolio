import type { Certificate } from "./types";

/**
 * Add certifications and credentials here when ready.
 */
export const certificates: Certificate[] = [];

export function getAllCertificates(): Certificate[] {
  return certificates;
}
